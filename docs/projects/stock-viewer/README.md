# Stock Viewer

**Path**: `projects/stock-viewer/`
**Domain**: stocks.supm3n.com
**Functions**: No (by default)
**Secrets**: `ALPHA_KEY` (only if used server-side)

Uses Chart.js. Inherits shared header/theme via `shared/*` includes (see Projects matrix). If you need to hide API keys, add a Pages Function proxy (see `docs/shared/ADD_FUNCTION.md`) and read the key from env.
